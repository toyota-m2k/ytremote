<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="android.view.View"/>
        <variable name="model" type="com.michael.ytremote.model.HostItemViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="5dp"
        android:paddingVertical="2.5dp"
        android:layout_marginVertical="2.5dp"
        android:background="@color/white"
        android:onClick="@{()->model.activate()}"
        >

        <ImageView
            android:id="@+id/check_mark"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:src="@drawable/ic_check"
            app:tint="@color/teal_200"
            android:visibility="@{model.isActive ? View.VISIBLE : View.INVISIBLE}"
            />
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toEndOf="@id/check_mark"
            android:layout_marginHorizontal="10dp"
            android:text="@{model.address}"
            tools:text="192.168.0.15:3500"
            />
        <ImageButton
            android:id="@+id/del_button"
            android:layout_width="40dp"
            android:layout_height="30dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:src="@drawable/ic_close"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:tint="@color/purple_500"
            app:rippleColor="@color/purple_200"
            android:onClick="@{()->model.remove()}"
            />



    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>